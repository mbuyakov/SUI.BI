package ru.sui.bi.core.enumeration

enum class ColumnType {
    BOOLEAN,
    INTEGER,
    DECIMAL,
    STRING,
    BINARY,
    DATE,
    TIME,
    TIME_WITH_TIMEZONE,
    TIMESTAMP,
    TIMESTAMP_WITH_TIMEZONE,
    OTHER
}